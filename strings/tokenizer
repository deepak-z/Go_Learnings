package main

import (
	"fmt"
"strings")

func TokenizeStringOnDelimiter(s string,delimiter string) []string{
	splitstrings := strings.Split(s, delimiter)
	return splitstrings
}

func main(){

	tpc := "BTC-INR"
	tpc_split := TokenizeStringOnDelimiter(tpc,"-")
	


	fmt.Println(tpc_split)
	/* a := []string{"1","2","3"}
	fmt.Println(a) */
}